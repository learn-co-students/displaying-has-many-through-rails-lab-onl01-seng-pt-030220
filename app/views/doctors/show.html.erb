<h1>Doctors</h1>
<h3><%= @doctor.name %></h3>
<h3><%= @doctor.department %></h3>
<% @doctor.appointments.each do |appt| %>
  <p><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
  <h3><%= link_to appt.patient.name, patient_path(appt.patient) %></h3>
<% end %>