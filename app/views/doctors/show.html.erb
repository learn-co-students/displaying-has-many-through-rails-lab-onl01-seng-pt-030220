<h1><%= @doctor.name %> Appointments</h1> <br>

<% @doctor.appointments.each do |a| %>
    <p><%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
    <%= link_to a.patient.name, a.patient %>
    </p>
<% end %>