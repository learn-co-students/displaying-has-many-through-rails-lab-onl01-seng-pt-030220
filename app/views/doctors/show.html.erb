<h1>Doctor:>

<% @doctor.appointments.each do |appt|%>
    <%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %H:%M"),appointments_path(appt) %> 
    <%= link_to appt.patient.name, patient_path(appt.patient.id) %>
<% end %> 

