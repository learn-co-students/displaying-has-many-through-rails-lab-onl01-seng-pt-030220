<% @doctor.appointments.each do |a|%>
<%=link_to a.appointment_datetime.strftime("%B %d, %Y at %H:%M"),appointments_path(a)%> 
<%=link_to a.patient.name, patient_path(a.patient.id)%>
<%end%> 


<%# a.appointment_datetime.strftime("%B %d %Y at %H:%M") %>