<h1>Appointments</h1>
  <h3><%= link_to @appt.doctor.name, doctor_path(@appt.doctor) %></h3>
  <h4><%= link_to @appt.patient.name, patient_path(@appt.patient) %></h4>
  <p><%= @appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>