<h2>Appointment: </h2>
<p><%= @appointment.format_date %></p>
<p><%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></p>
<p><%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></p>
