<h1>Patients</h1>

<%= @patient.name %>
<% @patient.appointments.each do |appt| %>
  <% @patient_doctor = @patient.doctors.find(appt.doctor_id) %>
  <%= link_to @patient_doctor.name, doctor_path(@patient_doctor) %> - <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
<% end %>