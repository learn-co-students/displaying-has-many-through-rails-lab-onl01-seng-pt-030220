<ul>
<% @patient.appointments.each do |appointment| %>
    <li>Appointment with <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> at <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
<% end %>
</ul>